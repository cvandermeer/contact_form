= ContactForm

Add to gemfile:

gem 'contact_form', git: 'git@github.com:cvandermeer/contact_form.git'

Then run:

rake contact_form:install:migrations

Then just create a new view based on your needs

This project rocks and uses MIT-LICENSE.
